import viteLogo from "../../public/vite.svg";
import "../App.css";

const HeaderBar = () => {
  return (
    <header>
      <img alt="react-logo" className="logo" src={viteLogo} />
    </header>
  );
};

export default HeaderBar;
